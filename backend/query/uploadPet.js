const zip_code = `
    SELECT * FROM ZIPCODE;
`

const getBreed = `
    SELECT * FROM BREED;
`

const healthIssue = `
    SELECT * FROM HEALTH_ISSUE;
`

module.exports = {
    zip_code,
    healthIssue,
    getBreed
}