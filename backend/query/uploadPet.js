const zipcode = `
    SELECT * FROM ZIPCODE;
`

const getBreed = `
    SELECT * FROM BREED;
`

const healthIssue = `
    SELECT * FROM HEALTH_ISSUE;
`

module.exports = {
    zipcode,
    healthIssue,
    getBreed
}